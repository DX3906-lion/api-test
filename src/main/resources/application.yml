spring:
  application:
    name: api-test-demo
  datasource:
    url: jdbc:mysql://localhost:3306/api_test_demo?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.apitest.entity

server:
  port: 8080
